

Apparently something is wrong in address parsing, or byte order conversion. ("127.0.0.1" -> "1.0.0.127")


./recv &
./recv

gives unhandled exception: Size.
(trying to listen on a port that's already in use -> size)