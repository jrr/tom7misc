
Installed node (which comes with npm) from binaries (zip) in d:\node

Put node in path:
in .bash_profile
export PATH="${PATH}:/d/node"

Install typescript:
npm install -g typescript
(this also seems to update to the newest version)

tsc --version

Version 4.2.4

