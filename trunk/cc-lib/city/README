This is Google's open source cityhash. I tried to make it a single-file 
compile by removing the dependency on config.h. Any modifications 
distributed under the same MIT License (see COPYING).

Local modifications:
  #include "city/city.h" instead of <city.h>

TODO:
  Add overloads for string_view, vector<uint8>?
  
